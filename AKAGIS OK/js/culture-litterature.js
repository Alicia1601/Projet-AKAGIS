// Gestion des boutons (les liens sont déjà définis dans le HTML)
document.querySelectorAll('.btn-section').forEach(button => {
    button.addEventListener('click', (e) => {
        // Les liens sont déjà définis dans le HTML, pas besoin d’action supplémentaire pour l’instant
    });
});