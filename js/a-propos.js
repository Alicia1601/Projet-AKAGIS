// Gestion des boutons "S’inscrire" et "Nous contacter"
document.querySelectorAll('.btn-join, .btn-contact').forEach(button => {
    button.addEventListener('click', (e) => {
        // Les liens sont déjà définis dans le HTML, pas besoin d’action supplémentaire pour l’instant
    });
});